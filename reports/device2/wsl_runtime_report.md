# D2-P0C WSL RUNTIME REPORT

## 1. Summary

Result: `caution`

D2-P0C completed the safety checks, WSL probes, storage policy, ignore-rule update, and runtime planning documents. Windows can see the NVIDIA RTX 4070 GPU, but WSL/Ubuntu and WSL-side GPU visibility are not confirmed. D2-P1 should not begin until Ubuntu and WSL CUDA readiness are fixed and verified.

## 2. D2-P0B Commit Status

D2-P0B was uncommitted at the start of D2-P0C. The worktree contained only the allowed D2-P0B files, so they were staged and committed locally.

Commit: `37e0cee` (`chore: add device2 environment readiness checks`)

## 3. Commands Run

```text
git status --short
git branch --show-current
git log --oneline -n 8
git -c safe.directory=C:/Users/Administrator/Documents/TCM-Ass/TCM-Assistant status --short
git -c safe.directory=C:/Users/Administrator/Documents/TCM-Ass/TCM-Assistant branch --show-current
git -c safe.directory=C:/Users/Administrator/Documents/TCM-Ass/TCM-Assistant log --oneline -n 8
git -c safe.directory=C:/Users/Administrator/Documents/TCM-Ass/TCM-Assistant status --short --untracked-files=all
git -c safe.directory=C:/Users/Administrator/Documents/TCM-Ass/TCM-Assistant add requirements-device2.txt scripts/device2/check_env.py reports/device2/env_check.json docs/DEVICE2_ENVIRONMENT_CHECK.md docs/DEVICE2_REQUIREMENTS_DRAFT.md reports/device2/env_check_report.md
git -c safe.directory=C:/Users/Administrator/Documents/TCM-Ass/TCM-Assistant -c user.name=Codex -c user.email=codex@example.com commit -m "chore: add device2 environment readiness checks"
wsl --status
wsl -l -v
wsl --version
where.exe wsl
wsl -l
wslconfig /l
wsl bash -lc "cat /etc/os-release"
wsl bash -lc "uname -a"
wsl bash -lc "nvidia-smi"
wsl bash -lc "which python3 || true"
wsl bash -lc "df -h"
wsl bash -lc "free -h"
nvidia-smi
nvidia-smi --query-gpu=name,memory.total,driver_version --format=csv,noheader
wmic logicaldisk get size,freespace,caption
Get-PSDrive -PSProvider FileSystem
Get-Volume | Select-Object DriveLetter,FileSystemLabel,FileSystem,SizeRemaining,Size
[System.IO.DriveInfo]::GetDrives()
python scripts/device2/check_wsl_cuda.py
python scripts/device2/check_env.py
git -c safe.directory=C:/Users/Administrator/Documents/TCM-Ass/TCM-Assistant diff --check
git -c safe.directory=C:/Users/Administrator/Documents/TCM-Ass/TCM-Assistant status --short
python -m py_compile scripts/device2/check_wsl_cuda.py
```

## 4. Windows Results

* GPU: `NVIDIA GeForce RTX 4070`
* VRAM: `12282 MiB`
* NVIDIA driver: `560.94`
* CUDA shown by Windows `nvidia-smi`: `12.6`
* `wmic logicaldisk`: unavailable (`logicaldisk - Alias not found`)
* `Get-Volume`: blocked by access denied
* `.NET DriveInfo`: succeeded

Drive inventory from `.NET DriveInfo`:

| Drive | Total | Free | Note |
| --- | ---: | ---: | --- |
| `C:\` | `199.19 GiB` | `22.63 GiB` | Repo drive; not suitable for model caches. |
| `D:\` | `377.00 GiB` | `76.75 GiB` | Usable for some artifacts after cleanup. |
| `E:\` | `376.50 GiB` | `126.92 GiB` | Best current target for model/cache storage. |

## 5. WSL Results

* `wsl.exe`: present at `C:\Windows\System32\wsl.exe`
* `wsl --status`: non-zero result with no usable readiness output
* `wsl -l -v`: non-zero result; returned WSL help/error output rather than a distro list
* `wsl --version`: non-zero result; treated as unsupported/not usable
* `wslconfig /l`: command not found
* Ubuntu: not confirmed
* WSL GPU visibility: not confirmed
* WSL `python3`, `pip3`, disk, and memory: not confirmed because WSL shell commands follow the WSL help/error path

Recommended fix before D2-P1:

```text
wsl --install -d Ubuntu-22.04
```

Ubuntu 24.04 is also acceptable if the machine's WSL tooling and future CUDA stack support it cleanly.

## 6. Storage Results

Model and runtime caches must stay outside the repo. Current recommendation prefers E:

```text
E:\ai_models\huggingface
E:\ai_models\modelscope
E:\ai_models\vllm
E:\ai_artifacts\tcm_assistant_device2
```

After Ubuntu exists, use the WSL paths:

```text
/mnt/e/ai_models/huggingface
/mnt/e/ai_models/modelscope
/mnt/e/ai_models/vllm
/mnt/e/ai_artifacts/tcm_assistant_device2
```

Fallback target if E is unavailable:

```text
D:\ai_models\huggingface
D:\ai_models\modelscope
D:\ai_models\vllm
D:\ai_artifacts\tcm_assistant_device2
```

Do not store base models, adapters, checkpoints, Hugging Face cache, ModelScope cache, or vLLM cache under `C:\Users\Administrator\Documents\TCM-Ass\TCM-Assistant`.

## 7. Files Changed

* `.gitignore`
* `scripts/device2/check_wsl_cuda.py`
* `docs/DEVICE2_WSL_RUNTIME_PLAN.md`
* `docs/DEVICE2_STORAGE_POLICY.md`
* `docs/DEVICE2_ENVIRONMENT_CHECK.md`
* `reports/device2/wsl_runtime_report.md`
* `reports/device2/wsl_cuda_check.json`
* `reports/device2/env_check.json`

`reports/device2/env_check.json` was regenerated by the requested legacy
`python scripts/device2/check_env.py` validation run. Its stage remains
`D2-P0B`; the updated values reflect the current D2-P0C worktree, current HEAD,
and current disk space.

## 8. Validation Results

* `python scripts/device2/check_wsl_cuda.py`: passed at command level; generated `reports/device2/wsl_cuda_check.json`; reported `caution`.
* `python scripts/device2/check_env.py`: passed at command level; regenerated `reports/device2/env_check.json`; reported `caution`.
* `git diff --check`: exit code `0`; only CRLF normalization warnings from Git.
* `python -m py_compile scripts/device2/check_wsl_cuda.py`: exit code `0`.

## 9. No-Go Confirmation

* Model downloaded: no
* Training started: no
* vLLM server started: no
* Business code changed: no
* API contract changed: no
* Schema changed: no
* Push performed: no

## 10. D2-P1 Readiness

Can enter D2-P1 now: no

Blocking items:

* Ubuntu is not confirmed in WSL.
* WSL-side `nvidia-smi` is not confirmed.
* WSL Python environment is not created or version-locked.
* External cache directories are planned but not created or exported.
* vLLM and training stack are intentionally not installed in D2-P0C.
