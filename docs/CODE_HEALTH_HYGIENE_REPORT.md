# P4.6.1 Safe Hygiene Report

Generated: 2026-06-20T09:42:59+08:00

## Scope

This phase applied only low-risk hygiene from the first Code Health Audit.

No business feature was added. No legacy, gate, SFT, RAG compatibility entrypoint was deleted or moved. No public API contract, SQLite schema, risk-rule semantics, or `TurnOutput` / `RunState` / `FinalReport` field semantics were changed.

## changed_files

Intentional hygiene changes:

| File | Change | Risk |
| --- | --- | --- |
| `requirements-dev.txt` | Added development-only code-health tool list. | safe |
| `app/chains/sft_infer_chain.py` | Removed unused imports: `Path`, `PeftModelForCausalLM`. | safe |
| `app/chains/report_chain.py` | Removed duplicate `TurnOutput` import. | safe |
| `scripts/audit_session.py` | Removed unused `sqlite3` import. | safe |
| `scripts/run_long_session_demo.py` | Removed unused `Optional` import. | safe |
| `docs/CODE_HEALTH_HYGIENE_REPORT.md` | Added this P4.6.1 report. | safe |
| `artifacts/code_health_hygiene.json` | Added machine-readable P4.6.1 report. | safe |

Validation side effects:

| File | Change | Reason |
| --- | --- | --- |
| `artifacts/p4_gate_result.json` | Regenerated by `python scripts/run_p4_gate.py`. | Required validation command. |
| `artifacts/p4_5_gate.json` | Regenerated by `python scripts/run_p4_gate.py`. | Required validation command. |

## safe_fixes_applied

- Added `requirements-dev.txt` so `ruff`, `mypy`, `vulture`, `radon`, `deptry`, and `pytest` remain development-only and are not added to runtime `requirements.txt`.
- Removed unused imports that had no known compatibility/re-export role.
- Removed one duplicate `TurnOutput` import in `app/chains/report_chain.py`.
- Verified edited Python files with `python -m py_compile`.

## untouched_risky_items

The following audit findings were intentionally left untouched:

| Item | Risk | Reason |
| --- | --- | --- |
| `DEFAULT_DB_PATH` import/re-export in `app/api/sqlite_store.py` | caution | It is imported by existing scripts, so removal could break compatibility. |
| Legacy MVP/stateful scripts and modules | caution | They are direct compatibility entrypoints and were not deleted or moved. |
| Gate helper duplication | caution | Consolidating helpers would be structural cleanup and is outside P4.6.1. |
| JSON parser duplication | caution | Shared parser extraction needs characterization tests first. |
| BM25/RAG tokenization and boost duplication | caution | Ranking behavior needs deterministic snapshots before consolidation. |
| High-complexity functions | risky | No function splitting or mainline refactor is allowed in this phase. |
| Mojibake/user-visible Chinese literals | risky | Fixing literals could alter user-visible API/report text and snapshots. |
| `TurnOutput`, `RunState`, `FinalReport` models | risky | Schema semantics are frozen for this hygiene pass. |
| FastAPI response models and API contract | risky | Public contract is frozen. |
| SQLite schema/constants | risky | SQLite schema is frozen. |
| Risk rules | risky | Risk-rule semantics are frozen. |
| Historical gate docs/artifacts | risky | They remain baseline evidence and were not deleted. |

## tooling_status

| Tool | Status | Notes |
| --- | --- | --- |
| `ruff` | unavailable | `python -m ruff --version` failed: module not installed. |
| `mypy` | unavailable | `python -m mypy --version` failed: module not installed. |
| `vulture` | unavailable | `python -m vulture --version` failed: module not installed. |
| `radon` | unavailable | `python -m radon --version` failed: module not installed. |
| `deptry` | unavailable | `python -m deptry --version` failed: module not installed. |
| `pytest` | unavailable | `python -m pytest --version` failed: module not installed. Current gates use `unittest`. |
| `requirements-dev.txt` | added | Development-only tool list; runtime dependencies unchanged. |

## validation_results

| Command | Status | Notes |
| --- | --- | --- |
| `python -m py_compile app\chains\sft_infer_chain.py app\chains\report_chain.py scripts\audit_session.py scripts\run_long_session_demo.py` | pass | Edited Python files compile. |
| `python -m compileall -q app scripts tests` | pass | Required validation passed. |
| `python scripts\run_p4_gate.py` | pass | Exit code 0. It emitted the existing `transformers` / PyTorch version warning. |
| `python -m unittest discover` | pass / empty | Exit code 0, but discovered 0 tests from the repository root. |
| `python -m unittest discover -s tests` | pass | Supplemental actual test-suite run: 268 tests passed in 182.587s. |
| `python -m json.tool artifacts\code_health_hygiene.json` | pass | Machine-readable report is valid JSON. |

## next_recommended_phase

P4.6.2 Caution Hygiene Prep:

- Install dev-only tools from `requirements-dev.txt` in a developer environment.
- Run `ruff`, `mypy`, `vulture`, `radon`, and `deptry` without auto-fixing.
- Add characterization tests before any helper consolidation.
- Decide whether README current-status drift should be corrected as docs-only cleanup.
- Keep encoding repair, high-complexity refactor, schema changes, and compatibility entrypoint deletion out of scope until explicitly approved.
